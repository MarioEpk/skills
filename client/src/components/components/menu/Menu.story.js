import React from "react";

import Menu from "./Menu";

const children = (
    <>
        children
    </>
);

export default [Menu, () => ({
    children,
    onClick: () => {},
    // items: [],
})];
